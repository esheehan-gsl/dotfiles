# Fix pbcopy and pbpaste
set-option -g default-command "reattach-to-user-namespace -l zsh"

# Change default prefix key
unbind C-b
set -g prefix C-f
bind C-f send-prefix

set -g default-terminal "screen-256color"

setw -g mode-mouse on
set -g mouse-select-pane on
set -g mouse-select-window on
set -g mouse-resize-pane on

bind a new-window
bind A new-window -c '#{pane_current_path}'

# Add vim-like splitting bindings
bind v split-window -h -c '#{pane_current_path}'
bind s split-window -v -c '#{pane_current_path}'
bind V split-window -h
bind S split-window -v

# Vim-like movement
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Open a man page in a new pane (based on kergoth's configuration)
bind / command-prompt "split-window -l 12 'exec man %%'"

#### COLOUR (Solarized 256)

# default statusbar colors
# set-option -g status-bg colour235 #base02
# set-option -g status-fg colour136 #yellow
# set-option -g status-attr default

# default window title colors
# set-window-option -g window-status-fg colour244 #base0
# set-window-option -g window-status-bg default
#set-window-option -g window-status-attr dim

# active window title colors
# set-window-option -g window-status-current-fg colour166 #orange
# set-window-option -g window-status-current-bg default
#set-window-option -g window-status-current-attr bright

# pane border
# set-option -g pane-border-fg colour235 #base02
# set-option -g pane-active-border-fg colour240 #base01

# message text
# set-option -g message-bg colour235 #base02
# set-option -g message-fg colour166 #orange

# pane number display
# set-option -g display-panes-active-colour colour33 #blue
# set-option -g display-panes-colour colour166 #orange

# clock
# set-window-option -g clock-mode-colour colour64 #green
 
run-shell "$POWERLINE_DAEMON_COMMAND -q"
run-shell "$POWERLINE_CONFIG_COMMAND tmux setup"
#source "/Users/evan/Library/Python/2.7/lib/python/site-packages/powerline/bindings/tmux/powerline.conf"
